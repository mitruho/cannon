<Projectile>:
    size: self.projectile_height, self.projectile_width
    canvas:
        Ellipse:
            pos: self.pos
            size: self.size
            source: 'default.jpg'
            
<Cannon>:
    size: 100, 30
    canvas.before:
        PushMatrix
        Rotate:
            angle: root.angle
            origin: self.x + 15, self.center_y
    canvas:
        Rectangle:
            pos: self.x + 15, self.center_y - 15
            size: self.size
            source: 'default.jpg'
    canvas.after:
        PopMatrix

<Target>:
    canvas:
        Ellipse:
            size: self.height, self.width
            pos: self.x, self.y
            source: 'default.jpg'
<CannonGame>:
    cannon: cannon
    projectile: projectile
    target: target
    Cannon:
        id: cannon
        angle: 0
    Projectile:
        id: projectile
    Target:
        id: target

